import React from 'react';
import { Routes, Route } from 'react-router-dom';

// 必要に応じてページコンポーネントをimportしてください
// 例: import Home from './components/Home';

const AppRoutes = () => (
  <Routes>
    {/* 例: <Route path="/" element={<Home />} /> */}
    {/* 必要なルートをここに追加 */}
  </Routes>
);

export default AppRoutes; 